-- See https://wiki.hypr.land/Configuring/Monitors/

-- hyprmon is used for configuration, this serves as a fallback
-- TODO find a hyprmon alternative

hl.monitor({
    output = "",
    mode = "preferred",
    position = "auto",
    scale = "auto"
})

