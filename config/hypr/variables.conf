# -- Applications --
$browser = zen-browser
$clipboardManager = walker -m clipboard
$fileManager = thunar
$passwordManager = proton-pass
$runMenu = walker
$terminal = kitty

# -- Keybinds --
$mainMod = SUPER

# -- Monitors --
# To list all available monitors: `hyprctl monitors all`
$monitor1 = DP-3
$monitor2 = HDMI-A-1
